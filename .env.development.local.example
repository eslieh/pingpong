# Copy to .env.development.local for local dev (overrides .env)
# Next.js loads this only in `npm run dev`.

NEXT_PUBLIC_WS_URL=ws://127.0.0.1:8081/ws

# On the VPS, also allow your dev page origin:
# ALLOWED_ORIGINS=...,http://localhost:8080
